-- Baseline : schéma déjà présent sur la base (historique Prisma Migrate absent jusqu’ici).
-- Cette migration ne modifie pas le schéma ; elle initialise uniquement la table _prisma_migrations.
SELECT 1;
